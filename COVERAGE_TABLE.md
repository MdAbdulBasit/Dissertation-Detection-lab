# DETECTION COVERAGE RESULTS

This table is the primary Chapter 4 findings artefact (`LAB_HANDOVER_PHASE2.md` §7a).
One row per technique. Fill it as each technique completes — do not batch it up at the end.

**Legend**
`Default detected?` — Yes / Parent-only / No, using stock Wazuh rules with no customisation
`Level` — Wazuh alert level (1–15)
`Sub-technique gap?` — did the default map to the parent only, while the custom rule mapped to the sub-technique? This column is the measured version of the dissertation's central claim.

> **⚠️ Always enumerate atomic test numbers before running.** They are global within a technique across
> all platforms and do **not** start at 1. T1087.001 lists 8–11 under `windows`; tests 1–7 are
> Linux/macOS. Passing a wrong number returns *"Found 0 atomic tests applicable to windows platform"*,
> which is indistinguishable from a broken ART install and cost an investigation on 2026-08-05.
>
> **Listed-under-windows ≠ targets-Windows.** T1087.001 test 11 is enumerated under the `windows`
> platform because it *executes from* Windows, but it targets an **ESXi host** that this lab does not
> have. It will always fail. Check what each test actually touches, not just which platform ART files
> it under, and exclude off-target tests from the denominator.
>
> ```powershell
> Invoke-AtomicTest <ID> -ShowDetailsBrief
> ```
>
> Record the applicable numbers in the `Atomic avail.` column. Exclude tests targeting other platforms
> (ESXi, Azure, AWS) — they fail for environmental reasons and must **not** be recorded as detection
> failures. Note the exclusion so the coverage figures stay honest.

---

| # | Technique | Tactic | EID | Atomic avail. | Default detected? | Default rule / level / ATT&CK | Custom rule | Detected after? | Custom level | Sub-technique gap? | Alert count (atk/benign) | Notes |
|---|-----------|--------|-----|---------------|-------------------|-------------------------------|-------------|-----------------|--------------|--------------------|--------------------------|-------|
| 1 | T1059.001 PowerShell | Execution | 1 | 6,13,14,15,16,17 (6 of 22) | **YES — correctly** | 92027 / **T1059.001**; **92057 / T1059.001 ("base64 encoded command")**; 92070, 92071 / T1047+T1059.001 | **NONE REQUIRED** | n/a | n/a | **No gap to close** | baseline **61 / 15** | **⭐ FIRST TECHNIQUE WHERE THE DEFAULT RULESET IS ADEQUATE — and deliberately no custom rule was written.** Four default rules map to T1059.001, and two of them fire in **both** classes: `92027` (21 atk / 10 ben) and `92057` "*Powershell.exe spawned a powershell process which executed a base64 encoded command*" (5 / 5). So stock Wazuh detects plain PowerShell execution **and** base64 obfuscation, at the correct technique. Writing a rule here would duplicate working detection and inflate the apparent contribution of the custom ruleset — the same discipline applied to `qwinsta` on T1033. **This makes T1059.001 the strongest single case for the ML triage layer in the whole study:** the rule is correct, it catches the obfuscation, and it *still* cannot separate an attacker from an administrator, because `92057` fires just as readily on the benign mirror's `-EncodedCommand` automation call. Rule precision is not the binding constraint here — context is. **⚠️ `92070`/`92071` are attack-only and that is a harness-lineage artefact:** `AtomicTestHarnesses` launches PowerShell via **WMI** (to control the exact command line), so those two rules — which require a WMI-created process and map to T1047+T1059.001 — can never fire on a mirror that spawns directly. Exclude from separability claims. **⚠️ 420 of 481 raw attack alerts (87%) were rule 92213 at LEVEL 15, produced by the harness itself:** `Add-Type` compiles inline C#, so `powershell.exe` writes `Temp\<rand>\<rand>.dll` (280) and `csc.exe` writes `Temp\<rand>\<rand>.cmdline` (140), tripping "executable dropped in folder commonly used by malware". Zero in benign — a perfect discriminator created entirely by the test tooling. Now excluded; see LABELLING_SCHEME.md. **Test set:** 7 excluded as **defective upstream** (exit 255 on all 5 runs, mismatched quotes in the atomic's own command string — a broken test, not a miss); 1,4,18,19,21,22 (Mimikatz/BloodHound/PowerUp/SOAPHound) excluded as runtime downloads of offensive frameworks that are really other techniques; 8, 20 as multi-technique (T1218.005, T1071); 5 as a UAC bypass (T1548.002 + T1112) mis-filed under PowerShell; 10,11,12 need distinct detection logic and are a candidate second pass. Test 6 retained but has an **external dependency** (download cradle via `MsXml2.XMLHTTP`) — a future failure is environmental. Tests 13–16 needed the `AtomicTestHarnesses` module via `-GetPrereqs`; before that they failed with "not recognized as the name of a cmdlet", indistinguishable from a technique going undetected |
| 2 | T1059.003 Cmd Shell | Execution | 1 | 1,2,5,6 (4 of 6) | **YES — correctly** | 92004 / **T1059.003**; 92032 / T1087+**T1059.003**; 92052 / **T1059.003**; 92005 / T1059 *(parent only)* | **NONE REQUIRED** | n/a | n/a | **No gap to close** | baseline **45 / 15** — of which **35 / 15** map to T1059.003 or its parent | **SECOND TECHNIQUE WHERE THE DEFAULT IS ADEQUATE — no custom rule written.** Three rules map to T1059.003 and two fire in **both** classes: `92004` (5 atk / 10 ben) and `92032` (15 / 5). `92052` is attack-only, `92005` maps to the **parent** T1059 only. **This is the clearest false-positive case in the study:** `92004` and `92032` key on `cmd.exe` lineage, so *every* technique in the set that shells out trips them regardless of intent — they fired in all six techniques' attack classes and in every benign mirror that uses `cmd.exe`. Rule logic cannot fix that; a command shell looks the same whoever runs it. **⚠️ STRUCTURAL FINDING — T1059.003 is a *container* technique.** Custom rule `100240` (T1033 `whoami`) fired 5× and the images show `calc.exe` 5, `wscript.exe` 5, `whoami.exe` 5. This was **misdiagnosed first as one bad atomic (test 3) and re-run with tests 1,2,5,6 — the contamination was identical** (44 → 45 alerts, same images). The real cause: a command shell must execute *something*, and the payload has its own ATT&CK mapping. The atomics write batch and VBScript files whose payloads are `whoami` (T1033) and `wscript` running a `.vbs` (**T1059.005**, a sibling sub-technique). **No test selection avoids this** — unlike Discovery techniques, where the command *is* the technique. So sibling-technique alerts in an Execution window are correctly-detected behaviour, not errors, and counts are reported split by whether they map to the technique under test. See LABELLING_SCHEME.md §3b. **`92213` → T1105** on the `.vbs` write is likewise a genuine file-drop detection, misattributed, and correctly **not** excluded. **Excluded test 4** (BlackByte print bombing): deliberate process-spawn flood, T1499 territory, and would swamp the class from one atomic on a memory-constrained endpoint |
| 3 | T1087.001 Local Acct Discovery | Discovery | 1 | 8, 9, 10 (11 excl.) | **Parent-only** ⚠️ *see data-gap note* | 92031 / L3 / T1087 | 100200 | Yes | L10 | **Yes** | **50 / 15** (5+5 windows, +120s buffer) — *`custom` phase only* | **⚠️⚠️ DATA GAP, FOUND 2026-08-09 DURING AN EXPORT-INTEGRITY CHECK — READ BEFORE CITING THIS ROW.** T1087.001 has **no `baseline` windows in `detonation_log.csv` at all**, not even superseded ones. It was the first technique run, during the session in which rule `100200` was written, and it predates the `ruleset_phase` convention — every one of its windows is labelled `custom`. Consequently the "Parent-only / `92031` → T1087" finding **is not reproducible from the committed dataset**: it rests on reading `92031`'s rule definition, plus contemporaneous observation that is no longer in the data. Worse, `92031` does not appear in the custom phase either, because `100200` (L10) outranks it (L3) and displaces it — so the current dataset contains **no instance of `92031` firing on this technique in either phase**. **Three options, in order of preference:** (1) re-run a genuine baseline with `100200` temporarily removed from `local_rules.xml` — ~40 min including two deploy cycles, and the only route to a reproducible claim; (2) restate the finding as inferred from rule definitions rather than measured, and mark it as such wherever the seven failure modes are listed; (3) drop parent-only from the taxonomy. **Option 2 is the minimum honest action and has been applied here; option 1 remains open in PROJECT_PLAN.** This is exactly the kind of defect that survives to submission because the *conclusion* is almost certainly correct — `92031` genuinely does map to the parent T1087 — and nobody re-checks whether the evidence for it is still in the dataset. | Rule tightened 2026-08-05 to require an enumeration subcommand. **FP source: Wazuh agent's own SCA module runs `net user`** (parent `wazuh-agent.exe`) — see LABELLING_SCHEME.md. Test 11 excluded: targets ESXi, environmental — **not** a detection failure. **2026-08-06: rule confirmed firing on the atomic (L10, tags attack/discovery/local) AND on the benign admin mirror (`net user`, `net localgroup administrators`) at the identical level — rule-based detection cannot separate the two classes. This is the measured motivation for the ML triage layer, not a rule defect.** Final counts from the balanced 5+5 run at 09:51–10:11 (Defender off both sides, mirror matching ART's `cmd.exe` lineage, +120s label buffer): **50 attack / 15 benign**, i.e. 10 and 3 alerts per window with no variance. Rule **100200 fires in both classes — 25 attack / 10 benign** — so the custom rule cannot separate legitimate account enumeration from the atomic, which is the intended finding. Earlier readings of this row (10/4, then 10/17) were wrong for two reasons since corrected: the attack class had only 1 window against benign's 5, and the +30s label buffer discarded late alerts — 4 of 5 benign windows appeared to produce *zero* alerts purely because theirs arrived 35–79s after `window_end`. **Alert volume is inflated 2× by `net.exe` spawning `net1.exe`** with identical arguments — every `net` invocation yields two process-creation events and therefore two alerts (5 atk invocations → 10; 2 benign → 4). Deduplicate on this before quoting alert volumes or building count-based ML features. **Coverage gap — rule 100200 is blind to cmdlet-based enumeration:** test 9's `Get-LocalUser` / `Get-LocalGroup` / `Get-LocalGroupMember` produced **no** alerts, because the rule keys on `net` with an enumeration subcommand. An adversary using only PowerShell cmdlets for the same sub-technique evades it. Candidate follow-up rule against Sysmon EID 1 PowerShell telemetry or Script Block Logging |
| 4 | T1082 System Info Discovery | Discovery | 1, 11 | 1,7,9,11,27,29,30,34,35,36,37,38,39,40 (14 of 30) | **No** | 92052 / L4 / **T1059.003**; 92032 / L3 / **T1087** | **100230, 100231, 100232, 100233** | **Yes — all four fire** | L8 | **N/A — T1082 has no sub-techniques; the gap here is worse, see notes** | baseline **147 / 26** · custom **187 / 29**, of which custom-rule **59 / 16** | **DEFAULT MISATTRIBUTES RATHER THAN MISSES.** No default rule maps to T1082 at all. What fires is generic execution telemetry: 92052 (`cmd.exe` spawned by a non-explorer/non-cmd parent → T1059.003) and 92032 (child of `cmd.exe /C` → **T1087 + T1059.003**). So T1082 activity is actively attributed to **T1087 Account Discovery and T1059.003 Command Shell** — wrong technique *and* wrong tactic (Execution, not Discovery). For an analyst this is worse than silence: it points the investigation at the wrong behaviour. Stronger finding than T1087.001's parent-only loss of precision. **⚠️ Rule 92213 (L15, T1105) is 100% harness-induced FP** — its pcre2 matches `.ps1` under `\AppData\Local\Temp\`, and PowerShell writes `__PSScriptPolicyTest_<rand>.ps1` there on every script invocation, so `Invoke-LabRun.ps1` triggers max-severity alerts by existing. Filter on `targetFilename`, not rule ID. See LABELLING_SCHEME.md. Excluded 14–23 (WinPwn — downloads external tooling, spans dozens of techniques), 24 (Azure), 31/32 (ESXi), 10 (Griffon JScript — different mechanism, revisit), 28 (hangs to ART's 120s timeout, inflating the window to 2m14s). **⚠️ ALERT COUNTS INVALID — BENIGN CLASS MUST BE RE-RUN.** 146 attack vs 1 benign, and that lone benign alert was rule 60702 (VSS idle timeout), unrelated ambient noise: the mirror produced **zero** technique-relevant alerts. Cause is a harness artefact, not a detection result — ART executes command_prompt tests as `cmd.exe /c "<cmd>"`, while the mirror ran the same commands directly from `powershell.exe`. Both rules that fired key on cmd.exe lineage rather than on discovery behaviour (92032 requires `parentImage=cmd.EXE` + `parentCommandLine` containing ` /C `; 92052 requires `originalFileName=cmd.EXE` with a non-explorer/cmd parent). Attack-side parents were 75 `cmd.exe` / 70 `powershell.exe`; benign side had **no** `cmd.exe` at all. So the classes were perfectly separable on "was cmd.exe involved" — a classifier would score ~100% by learning Atomic Red Team's wrapper and the FP-reduction claim would be vacuous. `Invoke-LabRun.ps1` mirrors now route native CLI through `cmd.exe /c` with `&`-chained commands to match ART's process lineage, and cmdlets through a child `powershell.exe`. **Final counts after three rounds of fixing: 146 attack / 25 benign, 5+5 windows, Defender off on both sides.** Rule 92032 now fires in **both** classes (75 atk / 15 ben) so the classes are genuinely confusable. Two residual harness artefacts remain and are handled at feature-extraction time rather than in the lab: (i) `92052` attack-only vs `92004` benign-only — same underlying event, differing only because ART invokes `cmd.exe` by bare name while PowerShell resolves the full path, so both are collapsed via `rule_canonical` and command lines are path-normalised; (ii) `92027` attack-only (5 obs.), since ART's PowerShell executor spawns a child `powershell.exe` — mirror now does the same, so this closes on the next run. **Volume asymmetry (146 vs 25) is a function of test-set size — 14 atomic tests against 4 mirror command groups — not of behaviour; normalise or exclude count features.** See BENIGN_ACTIVITY_PROTOCOL.md §2a. — **RESULT AFTER DEPLOYING 100230–100233 (measured 2026-08-06, 5+5 windows, phase `custom`):** all four rules fire, in **both** classes — atk 100232×15, 100231×10, 100230×5, 100233×2; ben 100230×5, 100231×4, 100232×4, 100233×4. **35 process-creation events are now detected that the default ruleset saw not at all** — `hostname.exe` and `reg.exe` host-identity queries produced zero alerts in the baseline phase, so this is added visibility rather than relabelling; a further 4 `wmic.exe` events fire 100231 *and* 92032 (dual attribution, correct technique now present alongside the generic one). **Counts above are at the corrected +120s label buffer.** An earlier read of this row reported custom-phase attack as 117 and blamed a drop from baseline on memory pressure causing incomplete atomics — **that was wrong.** The apparent drop, and the per-window variance behind it (14/18/35/21/29), were entirely an artefact of the old +30s buffer discarding late-arriving alerts. Measured forwarding lag reaches p99 111s and max 201s, so 31% of alerts were falling outside their own window. At +120s the custom phase reads **187** attack alerts against baseline's 147 — higher, not lower. See LABELLING_SCHEME.md for the lag distribution and buffer-sensitivity table. Established claims: (i) default attributes T1082 **zero** times across 5 baseline windows, (ii) all four custom rules detect it through four distinct mechanisms, (iii) all four also fire on legitimate administration — the `custom` phase shows **7 rules shared between classes and none exclusive to benign** — so rule logic alone cannot separate them, which is the measured warrant for the ML triage layer. **⚠️ CORRECTION 2026-08-09 — undeclared displacement found in the re-check.** `92027` went **5/0 → 0/0** while `100233` appeared, and the command lines are identical: `"powershell.exe" & {Get-CimInstance Win32_OperatingSystem}`. Those 5 alerts were **relabelled from `92027` (T1059.001) to `100233` (T1082)**, not newly detected — which is itself the correct outcome, since the behaviour is system-information discovery rather than PowerShell execution, but it must be reported as relabelling. `100233`'s other 5 alerts (`Get-WinSystemLocale`) are genuinely new. The row previously described `92027` as ambient activity that "happened to be present during the baseline window"; **that explanation was wrong** — it did not drift away, it was outranked. Corrected here rather than quietly amended |
| 5 | T1033 System Owner/User | Discovery | 1 | 1,4,5,6,7 (5 of 6) | **Partial** | 92022 / L3 / **T1033** — but only for `qwinsta` | **100240, 100241** | **Yes — both fire** | L8 | **N/A — gap is within the technique, see notes** | baseline **149 / 17** · custom **147 / 15**, of which custom-rule **15 / 10** | ⚠️ **Baseline attack corrected 165 → 149 on 2026-08-08.** The original figure included 16 rule-`92219` alerts that were Windows Update unpacking patches under `C:\Windows\SoftwareDistribution\Download\` — ambient OS activity, attack-only by coincidence of timing. Found while analysing T1547.001, where the same rule contributed 26. Excluded dataset-wide as `os-background:windows-update`. **THIRD DISTINCT DEFAULT-FAILURE MODE.** Not parent-only (T1087.001) and not outright misattribution (T1082), but **partial coverage of the dominant variant**. Default rule 92022 *correctly* maps `qwinsta.exe` session enumeration to T1033 (19 alerts) — where the default is right, it is recorded as right. But **`whoami.exe` produced 15 alerts, all rule 92032, which maps to T1087 + T1059.003** — so the single most common way anyone establishes identity on a host is attributed to Account Discovery and Command Shell rather than System Owner/User Discovery. The benign class fired **no** T1033-mapped rule at all. Custom rules therefore target only the gap: 100240 (`whoami`) and 100241 (PowerShell identity enumeration, atomics 4 and 5). **Deliberately no rule for `qwinsta`/`quser`** — 92022 already handles it, and duplicating correct default behaviour would inflate the apparent contribution of the custom ruleset. **Excluded test 3** (PowerView): downloads from GitHub at runtime *and* `Find-DomainUserLocation` needs a domain controller this WORKGROUP-only lab does not have. **Limitation:** atomic T1033-1 includes `wmic useraccount get /ALL`, which is arguably T1087.001 rather than T1033 — the atomic spans two techniques. **Clean-baseline check passed:** zero `1002xx` alerts leaked from the five already-deployed rules, so this is a genuine default-only measurement. — **RESULT AFTER DEPLOYING 100240–100241** (5+5 windows, gaps matched to baseline at 180–300s so the phases are comparable): **`100240` fires in BOTH classes — 5 attack / 10 benign.** `whoami` is now attributed to T1033 rather than T1087, and it fires just as readily on the administrator. Benign fires 10 because the mirror runs `whoami` twice per window while the atomics run it once. **⚠️ `100241` is attack-only (10 / 0) and that is a mirror-scope artefact, not a detection result** — the atomic set covers both the native and the PowerShell identity path while T1033's benign mirror covered only `whoami`. The mirror has since been widened to include `WindowsIdentity::GetCurrent` and `%USERNAME%`, but T1033's recorded data predates that, so treat `100241`'s class-exclusivity as an artefact and exclude it from any separability claim. **Attack total fell 165 → 147 between phases; this is not a rules effect** — rule 92219 (Windows Update DLL drops) went from 16 to 0 and 92027 from 15 to 5, both ambient activity that happened to be present during the baseline window. **Telemetry gap:** atomic T1033-7 exits 1 with no output and `whoami.exe` appears only 5 times across 5 attack windows — one per run, from test 1 — so tests 6 and 7 appear not to spawn `whoami.exe` at all. Verify or exclude them with a documented reason before the next run |
| 6 | T1016 Network Config Discovery | Discovery | 1 | 1,2,9 (3 of 7) | **No** | 92032 / L3 / **T1087+T1059.003**; 92052 / L4 / T1059.003; 92036 / **T1059.003+T1574.001** | **100250, 100251, 100252** | **Yes — all three fire** | L8 | **N/A — no correct mapping existed to lose precision from** | baseline **44 / 16** · custom **54 / 16**, of which custom-rule **33 / 12** | **NOTHING in the default ruleset maps to T1016.** Same failure mode as T1082 — activity is visible but attributed to Execution (T1059.003), Account Discovery (T1087) and DLL hijacking (T1574.001). Four techniques measured, **three** distinct failure modes, not four. **Cleanest phase comparison in the table:** both phases used identical 45–90s gaps and single-technique atomics, so the totals are directly comparable. **`100250` fires in BOTH classes — 18 attack / 12 benign.** **Precise rules displaced the misattributing generic rule:** `92032` fell from **23 → 5** between phases while 33 correct T1016 attributions appeared, so generic misattributed alerts dropped ~52% rather than simply being added to. **⚠️ `100251` and `100252` are attack-only and that is mirror scope, not a result** — the benign mirror runs `ipconfig`/`route`/`arp` but not `netsh show` or `net config`. Mirror since widened; T1016's data predates it, so exclude those two from any separability claim (same treatment as `100241` on T1033). **Test set narrowed to 1, 2, 9** — tests 4 (TrickBot) and 7 (Qakbot) are multi-technique recon sequences that also run `net`, `whoami` and `nltest`, which made single-technique attribution impossible; test 5 needs an external host, test 6 needs AdFind and Active Directory. See LABELLING_SCHEME.md §3a. **`100252` fires twice per `net config`** via the `net.exe`→`net1.exe` pairing, deduplicated at export. **⚠️ CORRECTION 2026-08-09 — a second, undeclared displacement.** The level-ordering discovery on T1547.001 (siblings are evaluated by descending level, so custom rules at L8 outrank vendor rules at L3–L6) prompted a re-check of every earlier technique. `92036` went **5/0 → 0/0** while `100252` went **0/0 → 5/0**, and the command lines are identical — `net  config` in both. So **5 of `100252`'s 33 custom-rule attack alerts are RELABELLED from `92036`, not newly detected**; the correct split is **28 new / 5 relabelled**. The `92032` 23→5 displacement was already reported; this one was not, and was found only by asking the question systematically rather than by noticing it |
| 7 | T1547.001 Registry Run Keys | Persistence | **13** (+11) | 1,3,7,12 (4 of 20) | **Rule exists, correctly mapped, and SILENT** | `92300` / **level 0** / T1547.001 — matches the Run key and emits nothing; only `92302` / L6 (writer is `reg.exe`), `92301` / L12 (`.lnk`/`.vbs`/`.vba`), `92303` / L12 (VNC) actually alert | **100290, 100291, 100292, 100294** *(100293 retired unreachable)* | **Yes — all four fire, and all four in BOTH classes** | L12 / L8 / L10 / L10 | **N/A — the gap is severity, not mapping** | baseline **27 / 35** · custom **35 / 45**, of which custom-rule **20 / 25** | **SIXTH DEFAULT-FAILURE MODE: SUPPRESSED SEVERITY — and the first technique where the BENIGN class is noisier than the attack class (27 attack vs 31 benign).** Predictions were **written into RULE_ID_REGISTER note 9 before detonating**, because the previous five modes were all characterised after seeing the data and were open to the charge of being fitted to it. Three of four confirmed, the fourth untestable with this test set and recorded as such rather than dropped. **Wazuh has a dedicated, correctly-tagged T1547.001 rule and it is deliberately mute.** `92300` matches `...CurrentVersion\Run` and carries `<mitre>T1547.001</mitre>`, but sits at **level 0** — matched, never emitted. It exists only to gate three narrow children. So a Run key holding a plain `.exe` path, or `powershell.exe -enc <payload>`, written by anything other than `reg.exe`, produces **nothing at all**. Distinct from every earlier mode: the rule exists (unlike T1082/T1016), the mapping is right (unlike T1136.001), the coverage is complete (unlike T1033), and it still cannot be seen. **⚠️ THE DECISIVE RESULT IS A WITHIN-TECHNIQUE CONTRAST.** Atomic 1 writes `HKCU\...\Run` via `reg.exe` → `92302` at L6, 4 alerts. Atomic 3 writes `HKLM\...\RunOnce` via PowerShell `Set-ItemProperty` → **zero** T1547.001 alerts. Same registry hive family, same persistence, same outcome at next logon; **detected or invisible depending only on which binary performed the write.** No earlier technique could demonstrate this, because the confound was always across techniques. Verified positively rather than by absence: the artefacts were confirmed present with `reg query` before cleanup, so the atomics definitely ran. **⚠️ AND THE ONE CORRECT RULE IS ANTI-DISCRIMINATIVE.** `92302` fires **4 attack / 5 benign** — more often on the administrator than the attacker, because registering an updater at logon via `reg.exe` is exactly what the benign mirror does. Every T1547.001-mapped alert in the dataset is a rule that favours the wrong class. **Second gap, separate from severity: PATH COVERAGE.** `92300`'s regex requires `RUN` immediately after `CURRENTVERSION`, so `HKCU\...\CurrentVersion\Policies\Explorer\Run` (atomic 12) never reaches it — a genuine Run-key location with no rule at all. It does loosely match `RUNONCE` and `RUNONCEEX`, since the alternation is unanchored. **Third gap: the Startup FOLDER variant is EID 11, not 13**, and nothing in `0860-sysmon_id_13.xml` covers files; atomic 7's `.lnk` drop produced no T1547.001 attribution. One technique, three independent gaps, one of them self-inflicted by a level setting. **MISATTRIBUTION ON TOP OF SILENCE:** `92041` L10 → **T1027 + T1112** (obfuscation / registry modification) and `92201` L9 → **T1105 + T1059** (ingress tool transfer). `92041` reads Windows Security event **4657**, not Sysmon EID 13 — it carries no `targetObject`/`details`, which is how it was identified. Both fire in **both** classes (4/5 and 5/6). **⚠️ 26 of the original 53 attack alerts were WINDOWS UPDATE** — rule `92219` "Possible DLL search order hijack", image `svchost.exe`, files under `C:\Windows\SoftwareDistribution\Download\`. Attack-only purely because an update ran during the attack phase. New exclusion class `os-background:windows-update` added; it removes **590 alerts dataset-wide** and retrospectively corrects **T1033 baseline attack from 165 to 149**. See LABELLING_SCHEME.md — this is the most dangerous artefact class found, because unlike the harness artefacts it is class-correlated by pure coincidence. **Test set:** 1 (`reg.exe`→Run), 3 (PowerShell→RunOnce), 7 (`.lnk`→Startup folder), 12 (Policies\Explorer\Run) — four mechanisms, deliberately chosen to isolate the writer rather than to maximise volume. **Excluded test 2** (`REG ADD` to `RunOnceEx` **without `/F`**): prompts `overwrite(Yes/No)?` on repeat and would have hung runs 2–5, the same collision that wrecked T1053.005 — caught in preflight this time instead of after the phase. **Excluded test 16** (`secedit` creating a Run key): the exporter already filters `secedit.exe` as Wazuh's own SCA activity, so that atomic would be silently discarded by our own noise filter and read as a detection failure. **⚠️ Benign phase re-run once**: the mirror's Startup-folder shortcut failed with `Missing ')' in method call` because `Start-Process -ArgumentList` does not re-quote array elements and Windows parsing consumed the quotes around a path containing a space. `Invoke-ViaPowerShell` fixed to escape and wrap; `20260808-070218-benign-r1` superseded with that reason. The same quote-stripping had been happening silently in every earlier mirror — it only mattered here because "Start Menu" contains a space. **⚠️ Baseline benign re-run 31 → 35** after a fifth mirror command was added (see below), so both phases share one identical mirror. — **RESULT AFTER DEPLOYING 100290/100291/100292/100294 (5+5 windows, same 150–210s gaps):** ⭐ **FIRST TECHNIQUE WITH ZERO CLASS-EXCLUSIVE CUSTOM RULES.** All four fire in *both* classes — `100290` 5/5, `100291` 5/10, `100292` 5/5, `100294` 5/5. The only exclusives left in the custom phase are `92052` (attack) and `92004` (benign), which are the long-known `cmd.exe` path-form artefact already collapsed by `RULE_CANONICAL`. Every previous technique left at least one custom rule stranded in one class as a mirror artefact (`100241`, `100251`, `100271`, `100282`); this is the first where that was **predicted and prevented before the run** by reading each new rule's regex against the mirror's own data. The 5/10 split on `100291` is the mirror working exactly as intended: the atomics write one plain-path Run value and one interpreter payload, the mirror writes two plain-path values and one interpreter payload, so `100290` lands 5/5 and `100291` lands 5/10. **⚠️ DISPLACEMENT IS SUBSTANTIAL AND MUST BE SPLIT OUT BEFORE QUOTING.** Wazuh evaluates sibling child rules by **descending level**, so the custom rules outrank the vendor's: `92302` (L6) went **4/5 → 0/0** and `92201` (L9) went **5/5 → 0/0**. Of 20 attack / 25 benign T1547.001-attributed alerts in the custom phase, **9 / 10 are RELABELLED** from those two rules and **11 / 15 are NEWLY VISIBLE** — events that previously produced no alert at all. Reporting 20/25 as new detection would overstate the contribution roughly twofold. **Levels were deliberately not lowered to avoid the displacement:** setting `100291` to level 5 would hand the `reg.exe` case back to `92302`, but choosing a severity to control evaluation order rather than to express severity would be bad detection engineering. **⚠️ AND `92201` VINDICATES THE BASELINE-FIRST METHOD.** Mid-technique I concluded from a smoke test that `92201` had never matched the Startup `.lnk` and "corrected" the analysis to blame `92200`. That was wrong — the smoke test ran *after* `100294` was deployed and already outranking `92201`, so it could say nothing about baseline behaviour. The phases settle it: `92201` 5/5 baseline → 0/0 custom, `100294` 0/0 → 5/5. **A post-deployment observation cannot be used to infer pre-deployment behaviour**, which is precisely why the baseline phase exists. **Vendor precision defect found in passing:** `92041` (L10, *"Value added to registry key has Base64-like pattern"*, → **T1027 Obfuscated Files**) fired on the plain path `C:\ProgramData\Smoke\viareg.exe`. It asserts obfuscation where there is none, which is why it sits at 4/5 and 5/5 across both phases and both classes — a level-10 alert that carries no information |
| 8 | T1053.005 Scheduled Task | Persistence | 1 | 1,2,4,6,9 (5 of 12) | **No** | 92027 / T1059.001; 92032 / T1087+T1059.003; 92052, 92004 / T1059.003 | **100270, 100271** | **Yes — both fire** | **L10** | **N/A — no correct mapping existed** | baseline **32 / 31** · custom **48 / 38**, of which custom-rule **28 / 4** | **TACTIC-LEVEL MISS — the starkest so far.** Nothing in the default ruleset maps to T1053.005, and *every* rule that fired maps to **Execution**: a Persistence technique read as Execution. Creating a task that survives reboot is materially different from running a command, yet an analyst would see only "powershell spawned powershell" with no indication anything was left behind. **Custom rules displaced the misattribution:** generic Execution rules fell **32 → 20** in the attack class while **28** correct T1053.005 attributions appeared. `100270` fires in **both** classes (15 atk / 4 ben) — creating a scheduled task is ordinary administration. **Level 10, not the 8 used for Discovery** — a scheduled task grants return access after reboot; severity tracks consequence. **⚠️ `100271` attack-only (13 / 0) is mirror scope, not a result** — the mirror creates tasks via `schtasks` but not via `Register-ScheduledTask`; exclude from separability claims (same treatment as `100241`, `100251`). **⚠️ FIRST TECHNIQUE WHERE THE FOUR-RUN LOOP FAILED.** These atomics create *named* persistent tasks, so run 1 created them and runs 2–5 measured **failed** creation: exit `0xC0000142`, a blocking `task already exists, replace? (Y/N)` prompt that timed out after 120 s, and `Register-ScheduledTask` returning "Cannot create a file when that file already exists". Six tasks were stranded. Fixed by adding `-CleanupBetweenRuns`, which runs ART cleanup in **dead time** — 130 s after `window_end`, past the 120 s label buffer, before the next window — so deletion telemetry is attributed to no window (confirmed: 40 unlabelled `schtasks` alerts). This forces gaps wider than the buffer, hence 180–300 s in both phases. **Baseline attack r1 superseded as an outlier** (191 s, 16 alerts against a consistent 8) — the pre-run cleanup did not prevent a first-iteration collision. **Rule-precision notes:** `100270` matches only `/create` and `/change` — `/query` is enumeration (and the mirror uses it), and `/delete` is **T1070.009** *and* what this lab's own cleanup runs 57 times, so a broad `schtasks` rule would have logged our cleanup as persistence attacks. Atomic 2 uses uppercase `/Create`, so `(?i)` is mandatory or a third of the atomics are silently missed. **Excluded tests:** 3 (needs remote host), 5 (VBA — T1059.005, needs Office), 7 (registry-staged payload — T1112), 8 (hidden attribute — T1564), 10 (Ghost Task registry surgery — T1112), 11/12 (MSC hijack — T1546.001) |
| 9 | T1136.001 Create Local Account | Persistence | 1 + **Security 4720** | 4,5,8,9 (4 of 9) | **Yes — but tagged the WRONG technique** | 60109 *"User account enabled or created"* / L8 / **T1098**; 60154/60160/60170 *"…Group Changed"* / **T1484**; 60111 / T1098+T1531 | **100280, 100281, 100282, 100283** | **Yes — all four fire** | L10 (100283 L8) | **Yes — sibling misattribution, see notes** | baseline **156 / 72** · custom **162 / 52**, of which custom-rule **105 / 34** | **FIFTH DISTINCT DEFAULT-FAILURE MODE — SIBLING MISATTRIBUTION, and the most insidious of the five.** The default ruleset detects the behaviour, describes it *correctly in plain English*, and then attaches the wrong ATT&CK ID. Rule `60109` fires on Windows Security event **4720** and is literally titled *"User account enabled or created"* — which is T1136.001 by definition — yet maps to **T1098 Account Manipulation**, a sibling. Worse, `60154`/`60160`/`60170` map *local* group membership changes to **T1484 Domain Policy Modification**, a domain mechanism this WORKGROUP endpoint does not possess. Distinct from parent-only (T1087.001), wrong-tactic (T1082/T1016/T1053.005), partial (T1033) and adequate (T1059.001/003). Insidious because the alert text reads correctly, so **a coverage matrix built from these mappings shows T1136.001 as uncovered while showing T1098 and T1484 as covered — both wrong.** **⚠️ REMEDIATION CLASS IS DIFFERENT AND CHEAPER.** `100280` and `100283` chain from the built-in rules via `if_sid` and add nothing but a corrected `<mitre>` block — the default rule still does the detecting. Contrast T1082 and T1016, where no default detection existed and rules had to be written from scratch. **Not every SIEM gap costs the same to close: some are one-line mapping errors, others are genuine blind spots, and a coverage matrix cannot tell them apart.** **DISPLACEMENT IS TOTAL AND EXACT** — `60109` 40/10 → **0/0** with `100280` picking up **exactly** 40/10; the three group rules 50/26 → **0/0** with `100283` at 50/20. 90 misattributed attack alerts relabelled, none lost, none duplicated. Deterministic because the atomics are. `92036` also fell 15 → 7 as `100281` displaced it. **⚠️ THE ASYMMETRY MATTERS MORE THAN THE DISPLACEMENT.** Displacement happens **only on the Security-log side**, where `if_sid` suppresses the parent. On the Sysmon side `100281` and the built-in `92039` **both** fire on the same `net user /add`, because `92039` is not a direct child of `sysmon_eid1_detections`. So the endpoint now emits one correct alert *and* one wrong one for the same action, and total alert volume goes **up** (156 → 162 attack). Left deliberately unfixed rather than papered over with `overwrite="yes"`: **bolting local rules onto a SIEM does not remove misattributed alerts, it adds correct ones alongside them, increasing analyst load in the short term.** That is the clearest argument in the study for why triage, not more rules, is the answer. **THREE OF FOUR CUSTOM RULES FIRE IN BOTH CLASSES** — `100280` 40/10, `100281` 10/4, `100283` 50/20. `100280` is the **strongest both-classes case in the project**: it is semantically perfect, keyed on the operating system's own "an account was created" event, and it is still **completely non-discriminating**, because creating a local account is exactly what the benign administrator mirror does. No amount of rule refinement can separate these — the behaviours are identical and only context differs. **⚠️ `100282` is attack-only (5 / 0) and that is a mirror-scope artefact, not a result** — the atomics create accounts via `New-LocalUser` and .NET `UserPrincipal`, the mirror only via `net user`. Same treatment as `100241`, `100251`, `100271`; exclude from separability claims. **Rule 100200 had TWO precision defects, both found by asking "which existing rule fires on the *next* technique's commands?"** — first it matched `net user X /add` (creation reported as discovery), then, after the fix, `net user X /del`, because Windows accepts the abbreviation. In T1136.001 windows 100200 went baseline 5/3 → custom **6/0**. **🔎 The vendor ruleset makes the identical error:** `92039 "net.exe account discovery command"` fires on `net user … /add` **and** `/delete`, and `92031 "Discovery activity executed"` on `net localgroup … /add` and `/delete`. All four are *modifications*, not enumeration. Finding the same defect in shipped Wazuh rules that I had twice in my own reframes the contribution: substring-matching a multi-purpose binary's command line without excluding the modifying switches is **structurally fragile**, not a personal slip. **⚠️ OPEN: `92039` does not appear in the labelled T1136.001 output at all**, despite being observed firing at 04:26:38 etc. inside baseline benign windows. Resolve before quoting the vendor-error finding. **Baseline benign class is contaminated and the custom benign class is not** — the between-runs cleanup block in `Invoke-LabRun.ps1` was missing the `-and $Type -eq 'attack'` guard the other two blocks had, so the baseline benign phase executed `Invoke-AtomicTest -Cleanup` and put ART deletions into benign telemetry (visible as 60111 8→5 and group rules 26→20). Guard added; custom benign run without the flag. Total gap time is unchanged (`130 + (gap-130) == gap`), so the phases remain comparable. **Timing held at 180–300s deliberately** — shortening mid-technique would change two variables between phases. **Smoke-test contamination check: 0 alerts** — `LabSmoke281` ended 05:04:04, attack-r1 opened 05:07:08, and nothing leaked. **Test set:** 4 (cmd prompt), 5 (PowerShell `New-LocalUser`), 8 and 9 (admin user, `net`/.NET) — 4 of 9; the rest are Linux/macOS. Tests 8 and 9 also add to Administrators, arguably T1098; retained because dropping half of four atomics is too thin and the risk was checkable rather than hypothetical |
| 10 | T1112 Modify Registry | Defense Evasion | **13 + FIM + 1** | 1,2,33,60 (4 of 90) | **Covered on paper, unusable in practice** | FIM `594`/`597`/`598`/`750`/`751`/`752` L5 — allow-listed, 12h scan; `92304`–`92306` — one UAC key, T1112 secondary; `91813`/`91814` — need Script Block Logging, **disabled**; `92041` L10 → **T1027**+T1112, a *process-creation* rule with a broken regex | **100260, 100261, 100262, 100263, 100264** | **Yes — but only after a SENSOR change** | L12 / L8 / L8 / L12 / L10 | **N/A — the gap is the sensor, not the mapping** | baseline **55 / 41** · custom **55 / 41** · **custom-sensor 93 / 55** | ⭐ **SEVENTH FAILURE MODE — LATENT COVERAGE — and the first technique in the study whose gap CANNOT be closed by a rule at all.** Predictions written into RULE_ID_REGISTER note 10 **before detonating**; all four held. **THREE PHASES, because the remediation has two independent parts and conflating them would have hidden the finding:** `baseline` (default rules, default sensor) → `custom` (custom rules, default sensor) → `custom-sensor` (custom rules, widened sensor). **THE RULE FIX CHANGED ATTRIBUTION, NOT VOLUME: baseline 55/41 → custom 55/41, identical.** `92041` 30/15 → **0/0**, `100264` 30/15 — an exact 1:1 replacement. **`92041` is the sharpest vendor defect found anywhere in this study.** It sits in `0800-sysmon_id_1.xml`, is a **process-creation** rule that never reads the registry, and its pattern `add.+\/d\s+(")?(?:[A-Za-z\d+\/]{4})*(?:[A-Za-z\d+\/]{3}=\|[A-Za-z\d+\/]{2}==)?` quantifies every group after `/d` with `*` or `?` — so zero repetitions satisfy it and it matches **any** `reg add … /d <anything>`, verified down to `add x /d ` with nothing following. It fired exactly once per `reg add` issued (6/run attack, 3/run benign) while asserting **T1027 Obfuscated Files — false in all 45 cases** — and it is **structurally blind to PowerShell registry modification**, since `Set-ItemProperty` spawns no `reg.exe`. A defect of *precision*, and precision defects are what generate alert fatigue: constant level-10 alerts carrying no information. `100263` restores a genuine base64 test and **fired zero times across all three phases** — correct, because no atomic writes a base64 value. **⚠️ THE BINDING CONSTRAINT IS THE SENSOR, MEASURED NOT ASSERTED.** `100260`/`100261`/`100262` are correct, deployed, and fire **0/0 in the custom phase** — because Sysmon's own config decides which registry keys emit EID 13 at all. Measured directly: 400 EID 13 events sampled, **81** Run-key writes, **0** for `HKCU\…\Policies\…\PowerShell\ModuleLogging`, **0** for `Internet Settings\ZoneMap`. So **two independent allow-lists stack**: Sysmon's include-list gates what exists, and Wazuh FIM's 20 HKLM paths on a 12-hour scan gate what is checked. **No SIEM rule can close a gap the sensor never emits.** Adding **9 lines** to the Sysmon config (zero removed, diffed line-by-line, two `TargetObject` entries only) moved those three rules from 0/0 to **20/0, 0/5 and 18/10**. Same rules, same atomics, same manager — only the sensor changed. **⚠️ THE COMMUNITY CONFIG ITSELF IS THE GAP.** The endpoint runs SwiftOnSecurity's `sysmonconfig.xml`, the most widely deployed community Sysmon config. Its registry include-list watches Defender exclusions, UAC tampering, Security Center and `Policies\Explorer\Run` — but **not** `Policies\Microsoft\Windows\PowerShell\`, the key that disables script-block and module logging. Atomic 33 disabled PowerShell logging on this endpoint and **nothing in the pipeline saw it**. **⭐ AND A CONTROLLED EXPERIMENT IN RULE DESIGN FELL OUT FOR FREE.** Two custom rules, same technique, same phase, same data source: `100260` keys on the **direction** of the change (DWORD set to zero = a control being disabled) → **20 attack / 0 benign**; `100262` keys on the **location** of the change (browser zone config) → **18 / 10**. The atomic disables PowerShell logging, the mirror enables it — same key, same writer, same rule surface. **The rule encoding *intent* separates the classes perfectly; the rule encoding *mechanism* cannot.** This is the **first cleanly discriminating custom rule in nine techniques**, and it is not a mirror artefact: disabling security logging is intrinsically adversarial in a way enabling it is not. It marks the boundary of what rule logic can do unaided, and is the sharpest available guide to what the triage model must learn. **⚠️ KNOWN LIMITATION OF `100260`, left in deliberately:** "disable" is not always zero — `EnableModuleLogging=0` disables, but `DisableAntiSpyware=1` also disables. Polarity depends on how the value name is phrased, so `100260` misses every `Disable*=1` setting, **including Defender tampering**. Enumerating the polarity of every security value name is the same brittle list-maintenance this technique criticises in the FIM allow-list, so it is recorded as a limitation rather than patched with a longer regex. **Test set 1/2/33/60** varies **hive × writer**: 1 = HKCU `Explorer\Advanced` via cmd; 33 = HKCU `Software\Policies\…\PowerShell` via cmd; 60 = HKCU `Internet Settings\ZoneMap` via PowerShell; 2 = HKLM `CurrentVersion\Run` via cmd, **on the FIM allow-list** and the discriminating case for prediction 3 — which held: an allow-listed path still produced **no** FIM alert in-window, so allow-list membership makes **no observable difference at incident timescales**. Test 2 also fires `100291` (T1547.001) within seconds while FIM sits on the identical event for up to 12 hours — the latency contrast on one host, kept deliberately and paired in the benign mirror so it cannot become a discriminator. Artefacts verified present with `reg query` before cleanup, because this technique predicts silence and "no alerts" cannot distinguish a working run from a broken one |
| 11 | T1218.011 Rundll32 | Defense Evasion | 1 | 9,15,16 (3 of 16) | **No — and one rule is structurally dead** | `92079` L10 → T1218.011 but gated behind `92078`, which needs `currentDirectory=E:\` **and** `cmd.exe`, and is itself mapped to **T1204.002**; `92081` L15 needs a `.html/.htm/.txt/.png/.jpg/.pdf` payload with `,#ordinal` | **100300, 100301, 100302** | **Yes — `100300` fires; 100301/100302 not exercised** | L10 / L6 / L12 | **N/A — no correct mapping existed to lose precision from** | baseline **32 / 24** · custom **≈30 / 15**, of which custom-rule **11 / 7** | **DEFAULT COVERAGE IS TWO RULES AND ONE CANNOT FIRE ON ANY NORMAL HOST.** `92079` is the only general-looking rundll32 rule and its parent `92078` requires an **E:\ current directory** — an ISO-mount scenario — so on a machine without that drive letter it is unreachable by construction. That leaves `92081`, which needs a document-extension payload with ordinal syntax. **All four pre-registered predictions held** (register note 11): no `92079`, no `92081`, **zero T1218.011 attribution in baseline**, and generic `92032`/`92052`/`92004`/`92027` firing instead — Defense Evasion read as Execution *and* Account Discovery. **⚠️ DISPLACEMENT IS TOTAL: `92032` went 10/12 → 0/0** while `100300` appeared at 11/7, so most of the custom-rule count is **relabelling**, not new detection — rundll32 proxy execution was previously being reported as "suspicious cmd shell execution / T1087 Account Discovery". **`100300` FIRES IN BOTH CLASSES (11 / 7), AS PREDICTED BEFORE THE RUN.** The mirror uses `pcwutl.dll,LaunchApplication` and `url.dll,FileProtocolHandler` — *the same exports as atomics 9 and 15* — because those genuinely are what the Program Compatibility Wizard and the Windows file-handler do. A rule keyed on the LOLBAS export list cannot separate the classes, and that is the honest result for a signed-binary-proxy technique: the binary, the DLL and the export are all legitimate; only the *target* differs, and the target is attacker-chosen. **⚠️ `100301` AND `100302` ARE "NOT EXERCISED", NOT "DID NOT FIRE" — the distinction is load-bearing.** `100301` (L6, any `DLL,Export`) is outranked by `100300` (L10) on every in-window rundll32 call, so it can only fire on invocations outside the LOLBAS list, of which there were none. `100302` (L12, `vbscript:`/`RunHTMLApplication`) has no atomic left after test 2 was excluded. **Both were proven correct by manual smoke test outside any detonation window**, using `-TimeoutSeconds 15` to bound the hang. Recording a working-but-unexercised rule as "found nothing" would be a false negative in the write-up. **Test set 9, 15, 16** — `pcwutl.dll,LaunchApplication`, `url.dll,FileProtocolHandler`, `zipfldr.dll,RouteTheCall`, each verified to exit code 0 before the phase. **Three atomics excluded, none of them a detection result:** **test 2** (`vbscript:`/`RunHTMLApplication`) **never terminates** — rundll32 persists as an HTA host until ART's 120s timeout, and its window measured **193s** against ~10s for the others, the same defect that excluded T1082's test 28; **test 12** downloads `calc.dll` from GitHub at runtime; **test 13** uses `copy` without `/Y` and prompts on repeat — **the third appearance of that trap** after T1053.005 and T1547.001. **⚠️ Four benign runs lost to a harness deadlock, root cause worth citing in the methodology:** `Start-Process -Wait` waits on a **job object**, so it blocks until every *descendant* exits. This mirror proxies `notepad.exe` through rundll32; cmd and rundll32 exited in milliseconds and the phase waited on a GUI window. Invisible until now because no earlier mirror launched a process that outlives its command. Fixed in both helpers with `-PassThru` + `WaitForExit(20000)`. **The near-miss is the instructive part:** the two mirror commands were tested in isolation with `-PassThru`+`WaitForExit`, both returned `True`, and that was read as proof the mirror was innocent — but that construct is *precisely the one that avoids the bug*, so the test could not have reproduced the fault. **A test must make the same call the code under test makes.** **⚠️ One benign custom window superseded**: attack r5 ended 12:56:13 and benign r1 opened 12:57:30, a 77-second gap against a 120s buffer. The attack→benign boundary must never be under ~3 minutes because contamination in that direction poisons the *negative* class, which no accuracy metric reveals. Caused by chaining the phases without the usual inter-phase wait to save time |
| 12 | T1070.004 File Deletion | Defense Evasion | **1 + 26 + FIM** | 4,7,9 (3 of 7) | **No — three layers each blind** | FIM `553` "File deleted." — user paths **not monitored at all**, 12h scan; `60117` covers event 1102 which is T1070.**001**; `61654` (EID 26) is **level 0 with no children** | **100310, 100311, 100312, 100313** | **Yes — but only 100311 usefully** | L8 / L10 / L8 / L10 | **N/A — no correct mapping existed** | baseline **33 / 20** · custom-sensor **79 / 48**, of which custom-rule **52 / 38** | **THREE INDEPENDENT LAYERS EACH FAIL TO SEE A FILE DELETION.** (a) FIM `553` reads syscheck, which watches only `%WINDIR%` restricted to three filenames, `%WINDIR%\SysNative` restricted to a fixed binary list, `drivers\etc` and `wbem`/`WMIC.exe` — **no user paths whatsoever** — on a 43200s scan. (b) `FileDelete` was absent from `sysmonconfig.xml` entirely, so EID 23/26 were never generated. (c) Even with the sensor enabled, base rule `61654` is **level 0 with no child rules**, so Wazuh emits nothing for a deletion regardless. Baseline T1070.004 attribution: **zero**. What fired instead was `92027`/`92052`/`92004` → T1059.x — Defense Evasion read as Execution for the fourth time. **⭐ THE HEADLINE RESULT — BOTH DATA SOURCES SEPARATE THE CLASSES, BUT ONLY AFTER THE SENSOR'S OWN OWNER IS EXCLUDED.** `100311` (EID 1, *command line* referencing `\Prefetch\`, `\winevt\Logs`, `wevtutil cl`, `usn deletejournal`, `vssadmin delete shadows`) fires **5 attack / 0 benign**. `100313` (EID 26, *file event* on the same paths) also fires **5 attack / 0 benign** — one per run, the atomic's Prefetch deletion by `cmd.exe`. **⚠️ CORRECTION, AND THE MOST INSTRUCTIVE MISTAKE IN THE PROJECT.** `100313` first measured **37 / 18** and I recorded the prediction as *failed*, concluding that file-event rules cannot capture intent because the OS performs the activity too. That conclusion was wrong. The 48 extra alerts were **my own rule matching `svchost.exe`'s continuous Windows Prefetch maintenance** — `POWERSHELL.EXE-920BBA2A.pf`, `WMIPRVSE.EXE`, `CMD.EXE`, `CONHOST.EXE`, `TRUSTEDINSTALLER.EXE`. Once negated, the rule reads exactly as predicted. **The real lesson is sharper and less comfortable: I drew a conclusion from a count of 37 when only 5 deletions had occurred, instead of asking why the number was wrong.** A rule that matches a location the operating system also writes to is *unmeasurable* until the OS is excluded, and any finding derived from it before that point is an artefact of the rule, not a property of the data. **⚠️ AND THE FIRST FIX MADE IT WORSE.** Negating `svchost.exe` on `100313` (L10) alone did not remove the noise — it **re-tiered** it into `100312` (L8), the catch-all with no image condition, which went 0/0 → 38/23. In a tiered ruleset an exclusion must be applied at **every tier that can match**, or upstream of all of them. Both rules now carry it, plus an `os-background:prefetch-housekeeping` filter in the exporter so historical re-exports agree with the corrected ruleset. Third self-inflicted noise source after `100262` (ZoneMap, 392 alerts) and `100313` itself — **and the first where the fix caused the next instance.** **`100310` fires 10 / 10** — both classes, as predicted; the mirror deletes a `.bat` via `cmd del` and a folder via `Remove-Item`, exactly as the atomics do. Deleting files is not adversarial. **`100312` is 0 / 10** and that was predicted *in the Sysmon config comment before the run*: the atomics delete **extensionless** artefacts (`deleteme_T1551.004`), which the `FileDeleteDetected` include list deliberately does not cover, while the mirror's `.bat` files are covered. **⚠️ HALF-TRUE SENSOR CEILING — the contrast with T1003.001 matters.** `100310`/`100311` read the EID 1 command line and work under the **default** sensor with no config change at all. T1003.001 has no alternative source — `ProcessAccess` is the only one. So "the sensor is the ceiling" is **fully** true for LSASS access and only **half** true for file deletion, where the sensor change adds file-level confirmation rather than the difference between blind and sighted. **⚠️ THE SENSOR CHANGE WAS SILENTLY DISCARDED ON THE FIRST ATTEMPT.** `sysmonconfig.xml` declared `schemaversion="4.50"`; `FileDeleteDetected` requires **4.60+**. Sysmon accepts a config with an older schema and **ignores elements that schema does not define** — it printed *"Configuration file validated"* and *"Configuration updated"*, and emitted no EID 26 at all. Caught only because the gate check (write a `.bat`, delete it, count EID 26) returned **zero**. Fixed by one attribute, `4.50` → `4.90` (installed Sysmon 15.21 reports 4.91). **"The config loaded" is never evidence that the config took effect — only probing for the event itself is.** **⚠️ `92213` AT LEVEL 15 FIRES 0 ATTACK / 10 BENIGN**, on the mirror's `.bat` files in Temp. The highest-severity alert this technique produces fires **exclusively on legitimate activity**. Partly the declared window-placement asymmetry (attack re-creates targets *before* the window via the new `-PrereqsEachRun`, the mirror must create them inside it), but severity 15 for a batch file in a temp directory is a precision defect regardless — third appearance of `92213` as pure noise after `__PSScriptPolicyTest` and `Add-Type`/`csc`, **551 alerts dataset-wide from one rule, none of them a detection**. **New runner switch `-PrereqsEachRun`**: these atomics *consume* their targets, so run 1 deletes the artefact and runs 2–5 would measure a failed deletion — third variant of the repeat-collision problem after T1053.005's named tasks and T1547.001's `REG ADD` without `/F`, and the first where the artefact is consumed rather than blocked. Prereqs are re-created *before* `window_start` so the creation telemetry falls outside the labelled window. **Test selection avoids a target collision:** tests 4 and 5 use the same path as a file and as a folder, and test 6 deletes the file test 4 already deleted, so any pairing of 4/5/6 would fail on a consumed target inside one window. **`100311`'s regex was corrected in the dry-run** — the first draft's `wevtutil\s+cl` and `vssadmin\s+delete\s+shadows` miss `wevtutil.exe cl` and `vssadmin.exe delete shadows`, because the `.exe` and often a closing quote sit between binary and subcommand. Same class of error as `100200`'s missing `/del`: written against how the command is *spoken about*, not how it appears on a real command line. **Per-window check:** attack-r1 ran 62s against ~7s for runs 2–5, but produced **8** alerts against 16–20 — the *low* outlier, so not contamination and not superseded |
| 13 | T1003.001 LSASS Memory | **Credential Access** | **10** | 2 (1 of 14) | **No — twice over** | `92900` / **L12** / T1003.001 — correctly written and **unreachable**; then still misses it once reachable | **100320, 100321** | **Yes — `100321` only** | L10 / **L12** | **N/A — no correct mapping was ever emitted** | baseline **5 / 4** · custom-sensor **16 / 10**, of which custom-rule **11 / 10** | ⭐ **THE CLEANEST SENSOR-CEILING RESULT IN THE STUDY, AND THE SHARPEST INTENT-VERSUS-MECHANISM PAIR.** `92900` is **level 12**, correctly mapped to T1003.001, and genuinely well written — `if_group sysmon_event_10`, `targetImage` matching `lsass\.exe`, `grantedAccess` in `0x1010|0x40`, `sourceImage` negated for `C:\Program Files` and `wmiprvse.exe`. Nothing is wrong with it. **It cannot fire**, because `ProcessAccess onmatch="include"` ships **empty** in SwiftOnSecurity's config — with its own comment noting an include with no rules logs nothing — so EID 10 is never generated. The atomic dumped LSASS five times and produced **zero** alerts of any kind. So the highest-value technique in the set is undetectable **not** because the rule is missing (T1082, T1016), **not** because it is mistagged (T1136.001), **not** because its severity is suppressed (T1547.001), and **not** because it arrives twelve hours late (T1112) — but because the sensor emits nothing to read. **⚠️⚠️ AND FIXING THE SENSOR IS NOT ENOUGH — `92900` STILL MISSES IT.** With EID 10 enabled and lsass being dumped, `92900` fired **zero** times while `100321` fired at level 12. Cause: `92900` requires `grantedAccess` in `0x1010|0x40`, and `comsvcs MiniDump` requests full access — `0x1fffff` or `0x1438` — so the vendor's own noise filter excludes **the most widely reported real-world variant of the technique**. A precision defect stacked on top of an unreachable rule; two independent reasons, and the second only became visible after fixing the first. `100321` uses a wider `grantedAccess` set. **⭐ THE TARGET IS NOT IN THE COMMAND LINE, AND THAT IS THE WHOLE FINDING.** The atomic runs `rundll32.exe comsvcs.dll, MiniDump <PID> <path> full` — the victim is a **PID**. From EID 1 it is *impossible* to know which process was dumped. The benign mirror runs the **character-identical command** against notepad's PID. Result: **`100320` (mechanism, EID 1) fires 5 / 5 — both classes, no separation possible.** **`100321` (target, EID 10) fires 6 / 0 — perfect separation.** Unlike T1112, where intent was visible in the *value written* (`DWORD 0` = a control disabled), here **intent is absent from process telemetry entirely**. Only the sensor's `targetImage` field carries it. So the sensor change is not a convenience — **it is the only route to intent**, and this is the strongest form of the argument in the dissertation. **⚠️ CONTRAST WITH T1070.004, MEASURED IMMEDIATELY BEFORE:** there `100310`/`100311` read the EID 1 command line and work under the **default** sensor, so the sensor change adds file-level confirmation rather than sight itself. **"The sensor is the ceiling" is fully true here and only half true there** — the two must be contrasted, not merged into one claim. **⚠️⚠️ A DEFECT IN THIS PROJECT'S OWN RULESET, FOUND ON TECHNIQUE 14 OF 15.** `100300` matches `comsvcs\.dll,\s*MiniDump` and the atomic runs exactly that — **and it did not fire.** `100300` chains from `if_group sysmon_eid1_detections`, which only exposes events a **vendor** 0800-series rule already matched. On T1218.011 rundll32 was spawned by `cmd.exe` and matched `92032`, entering the group; here the executor is `powershell`, so rundll32's parent is `powershell.exe`, no vendor rule matched, and the event never reached the group. Confirmed by inspection — the only images in the baseline attack windows were `svchost.exe` and `powershell.exe`, **no rundll32 at all**. **Every EID 1 custom rule written before this point** (`100200`, `100230`–`100233`, `100240`–`100241`, `100250`–`100252`, `100270`–`100271`, `100281`–`100282`, `100290`–`100294`, `100300`–`100301`, `100310`–`100311`, `100330`–`100332`) **inherits the vendor ruleset's blind spots** — change the parent process and the entire custom ruleset is bypassed without touching a detection condition. Reported as a limitation rather than silently patched: retro-fitting `if_group sysmon_event1` to fifteen rules would change measured counts for fourteen completed techniques and destroy cross-technique comparability. `100320`/`100321` use the correct parent, so this technique demonstrates **both the defect and its fix**. **⚠️ 1 of 14 atomics usable** — tests 1, 9 need ProcDump; 3, 4, 14 NanoDump; 6, 10 Mimikatz; 7 pypykatz; 8 `Out-Minidump.ps1`; 11 .NET `createdump`; 12 imported DLLs — all runtime downloads of offensive tooling, the exclusion applied consistently since T1059.001. Test 13's `rdrleakdiag.exe` was removed from recent Windows 10 builds. Test 2, `comsvcs.dll MiniDump`, is the only fully built-in variant — and the one most reported in the wild |
| 14 | T1560.001 Archive via Utility | Collection | 1/11 | | | | | | | | | |
| 15 | T1105 Ingress Tool Transfer | Command & Control | 3 | | | | | | | | | Transfer from Kali over labnet |

---

## Summary counters (recompute as you go)

| Metric | Value |
|--------|-------|
| Techniques attempted | **14 / 15 — all fourteen complete.** T1087.001, T1082, T1033, T1016, T1059.001, T1059.003, T1053.005, T1136.001, T1547.001, T1112, T1218.011, T1560.001, T1070.004, T1003.001. **Only T1105 remains** |
| Tactics covered | **6 of 7** — Discovery, Execution, Persistence, Defense Evasion, Collection, Credential Access. T1105 adds Command & Control |
| Sysmon event types exercised | **1, 10, 11, 13, 26** — plus Windows Security (4720/4732/4657), FIM (syscheck) and PowerShell EID 4104 examined and found unusable |
| Usable detonation windows | **258** · 66 superseded, every one with a written reason |
| Labelled dataset | **4,636 alerts kept — 2,431 in-window, 2,205 out.** Label buffer +120s, sized from measured lag (see LABELLING_SCHEME.md) |
| Custom rules deployed | **37** — blocks 100200, 100230–100233, 100240–100241, 100250–100252, 100260–100264, 100270–100271, 100280–100283, 100290–100294, 100300–100302, 100310–100313, 100320–100321, 100330–100332. `100293` retired as unreachable, ID reserved |
| **Techniques deliberately given NO custom rule** | **2** — T1059.001 and T1059.003, because the default ruleset is already correct. Writing rules there would have inflated the apparent contribution |
| **Eight distinct default-failure modes** | parent-only (T1087.001 *— inferred, see data-gap note*) · **wrong tactic** (T1082, T1016, T1053.005, T1218.011, T1560.001) · partial coverage (T1033) · **adequate** (T1059.001, T1059.003) · **sibling misattribution** (T1136.001) · **suppressed severity** (T1547.001) · **latent coverage, arrives 12h late** (T1112, T1070.004) · **unreachable — the sensor emits nothing** (T1003.001, and T1070.004's file-event layer) |
| ⭐ **The sensor ceiling has three shapes, not one** | **ABSOLUTE — T1003.001:** `92900` is L12, correctly mapped, correctly written, and cannot fire because `ProcessAccess onmatch="include"` ships empty. Once enabled it *still* misses comsvcs, whose `grantedAccess` its filter excludes. Two independent reasons. **PARTIAL — T1070.004:** three layers blind (FIM watches no user paths on a 12h scan, `FileDelete` absent from the sensor, base rule `61654` level 0 with no children) **but** EID 1 command-line rules work with no sensor change at all. **LATENCY — T1112:** coverage exists, is correctly mapped, and arrives up to 12 hours later. These must be reported separately; merging them overstates the claim |
| ⭐ **Intent vs mechanism — three measured pairs** | **T1003.001 is the purest:** the mirror runs a **character-identical** command against notepad, so `100320` (mechanism) is **5/5** while `100321` (target, EID 10 only) is **6/0**. Intent is **absent from process telemetry entirely** — only the sensor's `targetImage` carries it. **T1112:** `100260` (direction of change) **20/0** vs `100262` (location) **10/10**. **T1070.004:** `100311`/`100313` (forensic-artefact target) **5/0** vs `100310` (deletion as such) **10/10**. In every pair, the rule encoding *why* separates; the rule encoding *how* does not |
| Custom rules firing in **both** classes | **20 of 38 observed** — 100200, 100230–100233, 100240, 100250, 100262, 100264, 100270, 100280, 100281, 100283, 100290–100292, 100294, 100300, 100310, 100320. Ten attack-only, four benign-only, **and several of those are artefacts rather than results** — see the next two rows |
| ⚠️ **Class-exclusive ≠ discriminating — four distinct causes** | **(1) Mirror scope:** `100241`, `100251`, `100252`, `100271`, `100282` attack-only, and `100331` benign-only, because the mirror omitted a mechanism. **(2) Harness lineage:** `92070`/`92071` require WMI process creation that only `AtomicTestHarnesses` produces. **(3) Our own level ordering:** T1560.001's `100330` reads 0/5 but `100330 + 100332` sums to **5/5** — one mechanism split across two tiers by target. **(4) Genuine:** `100260`, `100311`, `100313`, `100321`, `100332` — these encode intent and separate for real. **Conflating (4) with (1)–(3) is the single easiest way to overstate this project's results** |
| ⚠️ **Every pre-T1003.001 EID 1 custom rule inherits the vendor's blind spots** | 15 rules chain from `if_group sysmon_eid1_detections`, which only exposes events a **vendor** rule already matched. Proven when `100300` failed to fire on `comsvcs.dll,MiniDump` because rundll32's parent was `powershell.exe` rather than `cmd.exe`. **Changing process lineage bypasses the entire custom ruleset without touching a detection condition.** Reported rather than patched: retro-fitting `sysmon_event1` to 15 rules would change counts for 14 completed techniques. `100320`/`100321` use the correct parent |
| ⚠️ **Sibling rules are evaluated by DESCENDING LEVEL** | Measured, not assumed. Custom rules at L8–L12 outrank vendor rules at L3–L9, so **every custom-phase count must be split into relabelled vs newly visible.** Full re-check completed: T1547.001 9/10 relabelled vs 11/15 new · T1136.001 90 alerts relabelled 1:1 · T1112 `92041` 30/15 · T1016 `92036`→`100252` 5 relabelled (28 new, not 33) · T1082 `92027`→`100233` 5 relabelled · T1218.011 `92032` 10/12→0/0 · T1560.001 **every** custom alert was relabelling, zero new detections. T1033 and T1053.005 clean |
| ⚠️ **Rules deployed but NOT EXERCISED** | `100263` (T1112), `100301`, `100302` (T1218.011) recorded 0/0 and were each proven correct by manual smoke test outside any detonation window. **"Not exercised" and "did not fire" mean opposite things** — recording the first as the second reports a working detection as a failure |
| **Vendor rule-precision defects found** | `92041` matches **any** `reg add … /d` and asserts T1027 obfuscation — wrong in 45 of 45 cases, and structurally blind to PowerShell registry writes · `92900` excludes comsvcs MiniDump via its `grantedAccess` filter, missing the technique's most reported variant · `92219` fires on Windows Update · `92213` at **level 15** fires 551 times dataset-wide, **not once a detection** · `92079` is gated behind an `E:\` current directory and can never fire on a normal host · `100200` had the same class of defect **twice** under my own authorship, which is what makes this structural rather than a vendor failing |
| ⚠️ **Self-inflicted noise — three instances, one caused by its own fix** | `100262` ZoneMap housekeeping **392 alerts**, swamping T1218.011 · `100313` Prefetch maintenance **48 alerts**, swamping T1003.001 · then negating `svchost` on `100313` alone **re-tiered 61 alerts into `100312`** instead of removing them. One root cause: **matching a location the OS also writes to without asking who else touches it.** In a tiered ruleset an exclusion must be applied at every tier that can match, or upstream of all of them |
| Alerts excluded as non-behavioural | **five documented classes** — harness-induced (PSScriptPolicyTest, Add-Type/csc) · agent self-monitoring · OS background (Windows Update) · ZoneMap housekeeping · Prefetch housekeeping. Plus `net.exe`→`net1.exe` duplicate removal |
| **Artefact classes in order of difficulty** | harness-induced (causal, traceable to a filename) · agent self-monitoring (causal, separable by lineage) · **OS background (no causal link at all — class-correlated by pure coincidence)** · **self-inflicted (created by our own sensor and rule changes)**. There is no procedure that finds the last two automatically; the only defence is treating **every class-exclusive rule as guilty until inspected** |
| **First technique where BENIGN is noisier than ATTACK** | **T1547.001 — 27 / 35.** Its only correctly-mapped rule, `92302`, fires 4 attack / **5 benign**. Also T1560.001 (10 / 16) and T1070.004's level-15 `92213` at **0 attack / 10 benign** |
| Techniques dropped / failed | **0.** Atomics excluded for documented environmental reasons: runtime downloads of offensive tooling, ESXi/Azure targets, non-terminating tests, prompts on repeat, and resource-prohibitive tests. Never recorded as detection failures |
| Methodology findings | ART harness fingerprint · multi-technique atomics · Execution techniques structurally multi-technique · label buffer sized from measured lag · `alerts.json` rotation · persistent-artefact collision on repeat · **artefact-consuming atomics needing `-PrereqsEachRun`** · **`Start-Process -Wait` job-object deadlock** · **Sysmon silently discarding config elements below the declared schema version** |
| ⚠️ **Export-integrity items open** | **(a)** T1087.001 has **no baseline windows** — its parent-only finding is inferred from rule definitions, not measured. **(b)** The repo dataset fell stale twice while the window log stayed current; now a four-step export with an assertion. Both found by deliberately auditing rather than by anything failing |


## Dropped or modified techniques

Record every deviation from the planned set and the reason. These become methodology and limitations
notes in the dissertation — an unexplained gap between the planned and delivered technique set is the
kind of thing an examiner asks about.

| Technique | Planned | What happened | Substituted with | Reason |
|-----------|---------|---------------|------------------|--------|
| | | | | |
